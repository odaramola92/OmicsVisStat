# OmicsVisStat - Python Dependencies
# Install with: pip install -r requirements.txt

# Core data processing
pandas>=1.3.0
numpy>=1.20.0

# Scientific computing and statistics
scipy>=1.7.0
statsmodels>=0.12.0

# Optional: R integration for limma support
# Requires R to be installed with limma package
# In R: install.packages('BiocManager'); BiocManager::install('limma')
# rpy2>=3.4.0  # Uncomment to enable limma analysis method

# Visualization
matplotlib>=3.4.0
seaborn>=0.11.0
venn>=0.1.3

# Machine learning and multivariate analysis
scikit-learn>=0.24.0

# Optional: Advanced machine learning (for enhanced ML capabilities)
# xgboost>=1.5.0  # Extreme Gradient Boosting (high performance)
# lightgbm>=3.3.0  # Light Gradient Boosting Machine (fast training)
# imbalanced-learn>=0.9.0  # Handling imbalanced datasets

# Excel file support
openpyxl>=3.0.0
xlsxwriter>=3.0.0
xlrd>=2.0.0  # For reading older .xls files

# Optional: Interactive plots (recommended)
plotly>=5.0.0

# Optional: Additional data formats
pyarrow>=5.0.0  # For feather format support

# GUI requirements (if running from source)
# tkinter is usually included with Python

# Build requirements (for creating executable)
# pyinstaller>=4.0  # Uncomment if building from source

# Quality of Life
tqdm>=4.60.0  # Progress bars (optional)
